/**
 * Observatory Page - v3.0
 * 
 * Public observatory wrapper page
 * Now uses the new canonical observatory system
 */

import { ObservatoryDashboard } from '../components/observatory/ObservatoryDashboard';

export default function Observatoire() {
  return <ObservatoryDashboard />;
}
